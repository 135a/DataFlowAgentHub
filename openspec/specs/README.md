# 主规格目录（`openspec/specs/`）

本目录用于存放**已通过归档 / sync 流程**合并进仓库主线的规格文件。

当前变更的 delta 规格位于 `openspec/changes/<change-name>/specs/`。在运行 `openspec-archive-change` 或 `openspec-sync-specs` 将变更合并到主线前，此处可以保持为空或仅保留本说明。
