## 1. 模式选择按钮

- [x] 1.1 创建 ModeSelector 组件，包含两个 pill 样式按钮（快速查询 / 深度分析），选中填充颜色，未选中镂空边框
- [x] 1.2 默认选中"深度分析"模式，点击切换选中状态
- [x] 1.3 选中态颜色：快速查询=蓝 `#2563EB`，深度分析=紫 `#7C3AED`；未选中态对应颜色边框+透明底
- [x] 1.4 默认模式持久化到 localStorage，页面加载时读取

## 2. 模式描述面板

- [x] 2.1 在输入框下方创建描述面板组件，根据选中模式显示不同文案
- [x] 2.2 快速查询文案："⚡ 快速查询：AI 生成 SQL 并直接执行返回结果，预计等待 1-3 秒"
- [x] 2.3 深度分析文案："🔬 深度分析：AI 将依次执行 SQL 生成→数据分析→图表绘制→报告生成，预计等待 5-15 秒"

## 3. 进度面板

- [x] 3.1 创建 ProgressPanel 组件，展示步骤列表（状态图标 + 步骤名 + 已用时间 + 进度条）
- [x] 3.2 快速查询定义 2 步：SQL 生成、执行查询；深度分析定义 4 步：SQL 生成、数据分析、图表绘制、报告生成
- [x] 3.3 监听现有 SSE 事件驱动进度：快速查询监听 sql_generated / result，深度分析监听 agent_step
- [x] 3.4 步骤状态切换：等待(⏳)→进行中(🔄)→完成(✅)
- [x] 3.5 步骤完成时对应进度条填满，下一步骤自动切换为进行中

## 4. 时间预估与历史记录

- [x] 4.1 首次查询使用硬编码默认时间作为初始预估
- [x] 4.2 查询完成后记录各步骤实际耗时到 localStorage（key: step_history）
- [x] 4.3 后续查询以历史平均耗时作为初始预估
- [x] 4.4 处理中实时显示已用时间和预估剩余时间（每秒刷新）

## 5. 集成到 App.tsx

- [x] 5.1 用 ModeSelector 替换现有 deepAnalysis checkbox
- [x] 5.2 用描述面板替换现有 sendStatus 文本区域
- [x] 5.3 集成 ProgressPanel，处理发送/完成/错误状态
- [x] 5.4 验证同步路径和异步路径均能正确展示进度
- [x] 5.5 验证 localStorage 读写正常，默认值回退正确
